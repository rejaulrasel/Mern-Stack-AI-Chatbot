import { AppBar, Toolbar } from '@mui/material';
import React from 'react';

const Header = () => {
    return (
        <div>
            <AppBar>
                <Toolbar></Toolbar>
            </AppBar>
        </div>
    );
};

export default Header;